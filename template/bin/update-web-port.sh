#!/bin/sh

CONFIG_FILE="${DOCKER_LIMS2_DIR}/sites/${SITE_ID}/labs/${LAB_ID}/config/system.php"

[ -e "$CONFIG_FILE" ] || cat <<EOF > $CONFIG_FILE
<?php

EOF

cat <<EOF >> $CONFIG_FILE

/* Generated by lims2-autodeploy */
if (defined('CLI_MODE')) {
    /* FIXME: Drop \`rum.genee.cn\`. */
    \$config['base_url']  = \$config['script_url'] = 'http://192.168.18.26:${WEB_PUB_PORT}/lims/';
}
\$config['local_api'] = 'http://127.0.0.1/lims/api';
EOF
